<% include header %>
  <body>
    <div class="container">
    <h1 class="title">Studddent.</h1>
    <p class="desc">Curated list of the best online student discounts for developers</p>
    <button type="button" class="btn addDiscount">Add A Discount</button>
    <hr>
    <div class="entries">
        <% posts.forEach(function(post){ %>
          <a href="<%= post.url %>">
          <div class="block">
              <div class="entry">
                  <div class="tnbg">
                    <img src="<%= post.image %>" alt="<%= post.name %>>"/>
                  </div>
                  
                  
                  <div class="middle">
                    <div class="headline"><%= post.name %></div>
                    <div class="url">
                      <script language='javascript'>
                        document.write(regURL('<%= post.url %>'));
                      </script>
                      </div>
                    <div class="feature"><%= post.feature %></div>
                  </div>
                  <div class="features">
                    <ul>
                      <% post.features.forEach(function(feature){ %>
                        <li><%= feature %></li>
                      <% }); %>
                    </ul>
                  </div>
                </div>
                <div class="voting">
                  <div class="up"><a href="javascript:upvote('<%= post._id %>')"><i class="fa fa-thumbs-up" aria-hidden="true"></i></a></div>
                  <div class="count"><%= post.votes.up - post.votes.down %></div>
                  <div class="down"><a href="javascript:downvote('<%= post._id %>')"><i class="fa fa-thumbs-down" aria-hidden="true"></i></a></div>
                </div>
            </div>
            </a>
        <% }); %>
    </div>
  <br><br><br><br><br><br>
  <p>TODO</p>
  <ul>  
      <li>set up voting</li>
      <li>sort by vote count</li>
      <li>create page for adding link</li>
      <li>launch</li>
    </ul>
    
  </body>
<% include footer %>
